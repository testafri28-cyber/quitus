{
  "name": "quitus-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tests end-to-end Playwright pour Quitus (parcours complets : auth, tickets, chat, présence, admin).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "dotenv": "^16.4.5"
  }
}
